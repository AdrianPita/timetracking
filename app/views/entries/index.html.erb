
<h1><%= @projects.name %></h1> 
	<ul> 
	<% @entries.each do |entry| %> 
		<li> 
			<%= entry.hours %> h 
			<%= entry.minutes %> m - 
			<%= entry.date %> 
		</li> 
		<% end %> 
	</ul>
	<p><%= pluralize(@projects.total_hours_in_month(Date.today.month, Date.today.year), 'hour', 'hours') %></p>